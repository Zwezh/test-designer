import { QuestionDetailsForm } from './question-details-form';

describe('QuestionDetailsForm', () => {
  it('should create an instance', () => {
    expect(new QuestionDetailsForm()).toBeTruthy();
  });
});
